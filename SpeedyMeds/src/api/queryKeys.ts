/**
 * React Query keys for various data types.
 */
export const queryKeys = {
  userProfile: ["userProfile"] as const,
  medicationReminders: ["medicationReminders"] as const,
  prescriptions: ["prescriptions"] as const,
  orders: ["orders"] as const,
};
