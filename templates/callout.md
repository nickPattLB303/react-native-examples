<!-- Callout Templates -->
<!-- Use Markdown blockquotes for callouts. -->
<!-- Custom styling can be applied via CSS later (e.g., adding icons, background colors). -->

---

**Note:**

> **Note:**
> Use this for general information, reminders, or side comments that are helpful but not critical.
> Example: Remember that Expo Go has limitations regarding custom native code.

---

**Tip:**

> **Tip:**
> Use this for helpful suggestions, best practices, or alternative approaches.
> Example: Consider using `useCallback` to memoize functions passed to child components to prevent unnecessary re-renders.

---

**Important:**

> **Important:**
> Use this for critical information that learners must pay attention to. This could relate to core concepts, security, or potential breaking changes.
> Example: Always provide a unique `key` prop when rendering lists of components.

---

**Warning:**

> **Warning:**
> Use this to highlight potential pitfalls, common mistakes, deprecated features, or actions that could lead to errors or unexpected behavior.
> Example: Modifying state directly without using the setter function (e.g., `this.state.count = 1`) will not trigger a re-render.

---

**Native Dev Context:**

> **Native Dev Context:** (For Android/iOS Developers)
> Use this to draw parallels or highlight differences compared to native Android (Kotlin/Java) or iOS (Swift/Objective-C) development.
> Example: In React Native, `StyleSheet` is analogous to defining XML layouts in Android or using Auto Layout/SwiftUI modifiers in iOS, but it uses JavaScript objects and a Flexbox-based layout system.

---

**Web Dev Context:**

> **Web Dev Context:** (For React/Angular/Vue Developers)
> Use this to relate concepts to web development, especially for those coming from React for the web or other frameworks.
> Example: React Native's `View` component is similar to the `div` element in HTML, serving as a fundamental container for layout and styling. The styling itself uses CSS-like properties but is applied via JavaScript objects rather than CSS files.