# Module 2: React Native Environment Setup with Expo

## 2.6 Anatomy of an Expo Project: Default Structure Walkthrough

Context: Understanding the default project structure created by npx create-expo-app@latest (using the default template) provides a roadmap for navigating and organizing code within an Expo application.40

Key Files and Folders:

-   app/: This directory is central to navigation when using the default template, as it houses the file-based routing logic managed by Expo Router. Each file or directory within app/ typically corresponds to a route in the application.40 Layout components (e.g., _layout.tsx) define shared UI structures like tab bars or headers for specific route segments.
-   assets/: Contains static assets used by the application, such as images (icons, logos), custom fonts, or potentially other media files.40
-   components/: An example directory intended for reusable UI components (e.g., custom buttons, cards, list items) that are used across different screens.40 This promotes modularity and code reuse.
-   constants/: An example directory for storing constant values used throughout the application, such as color definitions, API endpoints, or configuration keys.40
-   hooks/: An example directory for custom React hooks, encapsulating reusable stateful logic.40
-   node_modules/: This directory contains all the project's installed dependencies (libraries and packages). It is managed by the package manager (npm, yarn, pnpm) and should typically not be modified directly or committed to version control.
-   .gitignore: A standard Git configuration file listing files and directories that should be ignored by the version control system (e.g., node_modules/, build artifacts, environment-specific files).58
-   app.json / app.config.js: This is a crucial Expo configuration file.34 It defines metadata and settings specific to the Expo project, including the app's name, version, icon, splash screen, supported platforms, orientation lock, associated domains for deep linking, required permissions, and configuration for Expo plugins that modify native project settings.
-   babel.config.js: Configures the Babel JavaScript compiler. Babel is used to transpile modern JavaScript (ES6+) and JSX syntax into code compatible with the JavaScript engine (Hermes or JavaScriptCore). It's also where plugins for libraries like react-native-reanimated or expo-router are added.
-   eas.json: Configures Expo Application Services (EAS). It defines different build profiles (e.g., development, preview, production) specifying build settings like distribution methods, credentials, environment variables, and resource classes. It also configures EAS Submit settings for uploading builds to app stores.
-   metro.config.js: Configures the Metro bundler, React Native's default JavaScript bundler. This file allows customization of the bundling process, such as adding support for specific file extensions or modifying asset handling. Expo Router may require specific configurations here.
-   package.json: The standard Node.js project manifest file.34 It lists project metadata (name, version), dependencies (libraries required by the app), development dependencies (tools needed for development), and scripts for common tasks like starting the development server (start), running on specific platforms (android, ios, web), or linting.
-   package-lock.json / yarn.lock / pnpm-lock.yaml: These are lock files generated by the respective package managers. They record the exact versions of all installed dependencies, ensuring that installations are consistent and reproducible across different machines and environments. They should be committed to version control.
-   tsconfig.json: Configures the TypeScript compiler (tsc) options for the project, defining how TypeScript files are checked and compiled.58 It typically extends a base configuration provided by Expo (@tsconfig/react-native/tsconfig.json).

The default Expo project structure emphasizes convention and configuration. Directories like app/, assets/, and components/ provide organizational guidelines. Crucial aspects of the application's build process, metadata, native capabilities (via plugins), and cloud service interactions are managed through dedicated configuration files (app.json, eas.json, babel.config.js, metro.config.js, tsconfig.json). Familiarity with these files and the standard directory layout is essential for effectively developing, configuring, and building Expo applications.

#### Works cited

34. Reference - Expo Documentation, accessed April 24, 2025, <https://docs.expo.dev/versions/latest/>
40. Start developing - Expo Documentation, accessed April 24, 2025, <https://docs.expo.dev/get-started/start-developing/>
58. expo/expo-template-default: ⚠️ This is a mirror template repository for the new Expo default project template. Contributions and bug reports should be made in expo/expo repository. - GitHub, accessed April 24, 2025, <https://github.com/expo/expo-template-default>